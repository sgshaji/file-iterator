{
  "$comment": "Canonical column definitions for the three SharePoint lists used by the template regeneration solution. See docs/Template-Regeneration-Solution-Design.md section 4. Applied via provisioning/pnp-provisioning-template.xml.",
  "lists": [
    {
      "name": "DocumentIndex",
      "description": "One row per file in scope. Maintained by Flow E (backfill) and Flow F (delta). Read by Flow A to resolve affected documents without walking the tree.",
      "keyColumn": "UniqueId",
      "keyRationale": "SharePoint file GUID, not path. Paths change on folder rename; a path key silently produces duplicate rows, which means duplicate agent calls and duplicate cost.",
      "columns": [
        { "name": "UniqueId", "type": "Text", "required": true, "indexed": true, "enforceUnique": true, "description": "SharePoint file GUID. Primary key." },
        { "name": "FileName", "type": "Text", "required": true, "description": "Leaf file name including extension." },
        { "name": "ServerRelativeUrl", "type": "Note", "required": true, "description": "Full server-relative path of the file." },
        { "name": "ParentFolderUrl", "type": "Note", "required": true, "indexed": true, "description": "Server-relative path of the containing folder. Queried by Flow A." },
        { "name": "Extension", "type": "Text", "required": true, "indexed": true, "description": "Lower-case extension including the dot, e.g. .docx" },
        { "name": "SizeBytes", "type": "Number", "required": false, "description": "File size. Used by output validation size bounds." },
        { "name": "TimeLastModified", "type": "DateTime", "required": false, "description": "SharePoint last modified timestamp." },
        { "name": "Depth", "type": "Number", "required": false, "description": "Folder depth below the root folder. Diagnostics and walk sizing." },
        { "name": "DocumentRole", "type": "Choice", "required": true, "indexed": true, "choices": ["Source", "Output", "Template", "Archived"], "defaultValue": "Source", "description": "Source documents are regenerated. Outputs are written to. Templates trigger runs. Archived rows are previous output versions." },
        { "name": "TemplateName", "type": "Text", "required": false, "indexed": true, "description": "For outputs: which template produced this document. Drives cost filter F1." },
        { "name": "TemplateFingerprint", "type": "Text", "required": false, "description": "For outputs: fingerprint of the template version that produced this document. Drives cost filter F3 (skip already-current)." },
        { "name": "LastSeenRunId", "type": "Text", "required": false, "description": "Stamped by every backfill. Rows not seen by a completed backfill are stale, i.e. the file was probably deleted." },
        { "name": "IsExcluded", "type": "Boolean", "required": false, "defaultValue": "0", "description": "Row retained for audit but never selected as a regeneration source." },
        { "name": "IndexedAt", "type": "DateTime", "required": false, "description": "When this row was last written by Flow E or F." }
      ]
    },
    {
      "name": "RegenerationRun",
      "description": "One row per regeneration job. Created by Flow A, advanced by Flow B, closed by Flow C.",
      "keyColumn": "RunId",
      "columns": [
        { "name": "RunId", "type": "Text", "required": true, "indexed": true, "enforceUnique": true, "description": "GUID generated by Flow A." },
        { "name": "TemplateName", "type": "Text", "required": true, "indexed": true, "description": "Template whose publication triggered this run." },
        { "name": "TemplateFingerprint", "type": "Text", "required": true, "description": "Fingerprint of the template content at trigger time. Compared against the previous completed run to suppress no-op runs (cost filter F2)." },
        { "name": "TemplateVersion", "type": "Text", "required": false, "description": "SharePoint version label of the template file." },
        { "name": "Status", "type": "Choice", "required": true, "indexed": true, "choices": ["Planned", "AwaitingApproval", "Approved", "Running", "Completed", "CompletedWithErrors", "Cancelled", "RolledBack"], "defaultValue": "Planned", "description": "Run lifecycle. Flow B only claims items belonging to an Approved or Running run." },
        { "name": "PlannedCount", "type": "Number", "required": false, "defaultValue": "0", "description": "Work items created by Flow A after cost filters." },
        { "name": "SucceededCount", "type": "Number", "required": false, "defaultValue": "0" },
        { "name": "FailedCount", "type": "Number", "required": false, "defaultValue": "0" },
        { "name": "SkippedCount", "type": "Number", "required": false, "defaultValue": "0" },
        { "name": "AgentCallCount", "type": "Number", "required": false, "defaultValue": "0", "description": "HEADLINE COST METRIC. Reported by Flow C. If this approaches PlannedCount on every run, the cost filters are not working." },
        { "name": "IsDryRun", "type": "Boolean", "required": false, "defaultValue": "0", "description": "Plan-only run. Work items are created for inspection and never executed." },
        { "name": "RequiresSecondConfirmation", "type": "Boolean", "required": false, "defaultValue": "0", "description": "Set when PlannedCount exceeds MaxDocumentsPerRun. Circuit breaker." },
        { "name": "RequestedBy", "type": "Text", "required": false },
        { "name": "ApprovedBy", "type": "Text", "required": false },
        { "name": "StartedAt", "type": "DateTime", "required": false },
        { "name": "CompletedAt", "type": "DateTime", "required": false },
        { "name": "SummaryMessage", "type": "Note", "required": false, "description": "Written by Flow C." }
      ]
    },
    {
      "name": "RegenerationWorkItem",
      "description": "The work queue. One row per document per run. Written by Flow A, claimed and updated by Flow B. Persisting this state is what makes the job resumable, bounded in run duration, and immune to the 16 MB action output cap.",
      "keyColumn": "SourceUniqueId + RunId",
      "columns": [
        { "name": "RunId", "type": "Text", "required": true, "indexed": true, "description": "Groups items into a run. Queried by Flow B every cycle, so it must be indexed." },
        { "name": "SourceUniqueId", "type": "Text", "required": true, "indexed": true, "description": "SharePoint GUID of the source document. Idempotency key." },
        { "name": "SourceUrl", "type": "Note", "required": true, "description": "Server-relative path of the source document." },
        { "name": "SourceSizeBytes", "type": "Number", "required": false, "defaultValue": "0", "description": "Size of the source document, copied from the index by Flow A. Flow B's output validation compares generated size against this to detect truncation and runaway generation, so it must be populated at plan time." },
        { "name": "TargetUrl", "type": "Note", "required": true, "description": "Server-relative path where the regenerated output is written." },
        { "name": "ArchiveUrl", "type": "Note", "required": false, "description": "Where the previous output was archived before overwriting. Flow D rollback depends entirely on this." },
        { "name": "TemplateName", "type": "Text", "required": true },
        { "name": "TemplateFingerprint", "type": "Text", "required": true, "description": "Recorded so cost filter F3 can skip already-current outputs on a re-run." },
        { "name": "Status", "type": "Choice", "required": true, "indexed": true, "choices": ["Pending", "InProgress", "Succeeded", "Failed", "Skipped"], "defaultValue": "Pending", "description": "Queried by Flow B every cycle, so it must be indexed." },
        { "name": "AttemptCount", "type": "Number", "required": false, "defaultValue": "0", "description": "Bounded by MaxAttempts, then parked as Failed. Unbounded retry on a deterministic failure exhausts quota." },
        { "name": "ErrorMessage", "type": "Note", "required": false },
        { "name": "ValidationResult", "type": "Note", "required": false, "description": "Why an agent response was rejected before writing." },
        { "name": "ClaimedAt", "type": "DateTime", "required": false, "description": "Used with StaleClaimMinutes to reap items stranded by a crashed batch." },
        { "name": "ClaimedBy", "type": "Text", "required": false, "description": "Flow run identifier that claimed this item. Prevents double processing." },
        { "name": "CompletedAt", "type": "DateTime", "required": false }
      ]
    },
    {
      "name": "WalkFrontier",
      "description": "The folder walk frontier for Flow E. Persisted rather than held in an array variable so the walk is resumable after a failed run, observable while running, and immune to the 16 MB action output cap.",
      "keyColumn": "FolderUrl + WalkRunId",
      "columns": [
        { "name": "WalkRunId", "type": "Text", "required": true, "indexed": true, "description": "Groups frontier rows into one backfill pass." },
        { "name": "FolderUrl", "type": "Note", "required": true, "description": "Server-relative path of a folder awaiting enumeration." },
        { "name": "Depth", "type": "Number", "required": false, "description": "Depth below the root folder. Supports breadth-first ordering." },
        { "name": "Status", "type": "Choice", "required": true, "indexed": true, "choices": ["Pending", "InProgress", "Visited", "Failed"], "defaultValue": "Pending", "description": "Queried every chunk, so it must be indexed." },
        { "name": "ErrorMessage", "type": "Note", "required": false, "description": "Recorded when a folder cannot be enumerated, e.g. a single folder with more than 5,000 direct children throttling its own call." },
        { "name": "VisitedAt", "type": "DateTime", "required": false }
      ]
    }
  ]
}
