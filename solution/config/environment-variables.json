{
  "$comment": "Environment variable definitions for the template regeneration solution. Every operational value is here rather than hardcoded in a flow, so behaviour can be tuned, throttled or halted per environment WITHOUT a redeploy. This also removes the configuration-entry error class that consumed half of Day 1 of the enumeration investigation (see docs/SharePoint-Large-Library-File-Enumeration-Investigation.md section 7). See docs/Template-Regeneration-Solution-Design.md section 13.",
  "environmentVariables": [
    {
      "schemaName": "fi_SiteAddress",
      "displayName": "Site Address",
      "type": "String",
      "required": true,
      "defaultValue": "",
      "example": "https://contoso.sharepoint.com/sites/DemoFiles",
      "usedBy": ["A", "B", "D", "E", "F"],
      "description": "Site containing the document library. No trailing path beyond the site name."
    },
    {
      "schemaName": "fi_LibraryUrlName",
      "displayName": "Library URL Name",
      "type": "String",
      "required": true,
      "defaultValue": "Shared Documents",
      "example": "Shared Documents",
      "usedBy": ["A", "B", "D", "E", "F"],
      "description": "The library's URL name, which is frequently different from its display name (Documents vs Shared Documents). Use the value in the browser address bar."
    },
    {
      "schemaName": "fi_WebServerRelativeUrl",
      "displayName": "Web Server-Relative URL",
      "type": "String",
      "required": true,
      "defaultValue": "",
      "example": "/sites/DemoFiles",
      "usedBy": ["A", "F"],
      "description": "Server-relative URL of the SharePoint web, with a leading slash and no trailing slash. Prefixes connector trigger tokens, which omit the /sites/<web> segment."
    },
    {
      "schemaName": "fi_RootFolderPath",
      "displayName": "Root Folder Path",
      "type": "String",
      "required": true,
      "defaultValue": "",
      "example": "/sites/DemoFiles/Shared Documents/001NH",
      "usedBy": ["A", "E"],
      "description": "Server-relative path of the source root. The walk starts here."
    },
    {
      "schemaName": "fi_TemplateFolderPath",
      "displayName": "Template Folder Path",
      "type": "String",
      "required": true,
      "defaultValue": "",
      "example": "/sites/DemoFiles/Shared Documents/001NH/Template",
      "usedBy": ["A"],
      "description": "Server-relative path of the template folder. MUST be disjoint from the output folder, otherwise the solution's own writes re-trigger Flow A."
    },
    {
      "schemaName": "fi_OutputFolderName",
      "displayName": "Output Folder Name",
      "type": "String",
      "required": true,
      "defaultValue": "AD Documents",
      "usedBy": ["A", "B", "E"],
      "description": "Folder that receives regenerated documents. Never walked as a source, but paths are computed into it."
    },
    {
      "schemaName": "fi_ArchiveFolderName",
      "displayName": "Archive Folder Name",
      "type": "String",
      "required": true,
      "defaultValue": "Archive",
      "usedBy": ["B", "D", "E"],
      "description": "Folder that receives the previous version of every overwritten output. Never walked as a source."
    },
    {
      "schemaName": "fi_ExcludedFolderNames",
      "displayName": "Excluded Folder Names",
      "type": "String",
      "required": true,
      "defaultValue": "[\"Archive\",\"AD Documents\"]",
      "usedBy": ["E"],
      "description": "JSON array of folder names never walked as sources, wherever they appear in the tree. Held as configuration so exclusions can change without editing the flow. Excluded subtrees are dropped before enqueueing, so a subtree of any size costs nothing."
    },
    {
      "schemaName": "fi_SourceExtensions",
      "displayName": "Source Extensions",
      "type": "String",
      "required": true,
      "defaultValue": "[\".pdf\",\".docx\"]",
      "usedBy": ["A", "E", "F"],
      "description": "JSON array of lower-case extensions supported by the deployed PD Conversion Assistant skill. Legacy .doc files must be migrated to .docx before entering the source tree."
    },
    {
      "schemaName": "fi_BatchSize",
      "displayName": "Batch Size",
      "type": "Number",
      "required": true,
      "defaultValue": 5,
      "usedBy": ["B"],
      "description": "Documents claimed per Flow B run. THROTTLE 1 OF 2. Set from measured worst-case agent latency, not the average: one slow document stalls a sequential loop. Starts deliberately low for the pilot."
    },
    {
      "schemaName": "fi_PlanningPageSize",
      "displayName": "Planning Page Size",
      "type": "Number",
      "required": true,
      "defaultValue": 200,
      "usedBy": ["A2"],
      "description": "DocumentIndex rows consumed per planner run. Keeps planning below the 16 MB per-action output cap while guaranteeing eventual coverage beyond 5,000 rows."
    },
    {
      "schemaName": "fi_MaxDocumentsPerRun",
      "displayName": "Max Documents Per Run",
      "type": "Number",
      "required": true,
      "defaultValue": 50,
      "usedBy": ["A"],
      "description": "CIRCUIT BREAKER. Above this count the run is held for a second, deliberate confirmation. Stops a wrong template or a misconfigured filter becoming a large bill."
    },
    {
      "schemaName": "fi_StaleClaimMinutes",
      "displayName": "Stale Claim Minutes",
      "type": "Number",
      "required": true,
      "defaultValue": 60,
      "usedBy": ["B"],
      "description": "Items left InProgress beyond this are reset to Pending at the start of each cycle. Without reaping, a crashed batch strands its claimed items forever."
    },
    {
      "schemaName": "fi_WalkFolderChunkSize",
      "displayName": "Walk Folder Chunk Size",
      "type": "Number",
      "required": true,
      "defaultValue": 100,
      "usedBy": ["E"],
      "description": "Folders processed per Flow E run before re-queueing and exiting. Keeps any single run well inside the duration limit regardless of tree size."
    },
    {
      "schemaName": "fi_RollbackPageSize",
      "displayName": "Rollback Page Size",
      "type": "Number",
      "required": true,
      "defaultValue": 25,
      "usedBy": ["D2"],
      "description": "Side-effect manifests restored per rollback worker run."
    },
    {
      "schemaName": "fi_DryRun",
      "displayName": "Dry Run",
      "type": "Boolean",
      "required": true,
      "defaultValue": true,
      "usedBy": ["A"],
      "description": "When true, Flow A plans work items for inspection and the run is never executed. Zero agent cost. DEFAULTS TO TRUE so a fresh deployment cannot regenerate documents before anyone has looked at the plan."
    },
    {
      "schemaName": "fi_AgentId",
      "displayName": "Agent Schema Name",
      "type": "String",
      "required": true,
      "defaultValue": "cree1_pdconversionassistant_08zNQw",
      "usedBy": ["B"],
      "description": "Schema name of the existing PD Conversion Assistant agent invoked through shared_agentnode/InvokeAgent. The agent is referenced, not copied into this solution."
    },
    {
      "schemaName": "fi_IndexListName",
      "displayName": "Index List Name",
      "type": "String",
      "required": true,
      "defaultValue": "DocumentIndex",
      "usedBy": ["A", "E", "F"],
      "description": "Name of the document index list. Parameterised so a test deployment can point at its own copy without touching production data."
    },
    {
      "schemaName": "fi_RunListName",
      "displayName": "Run List Name",
      "type": "String",
      "required": true,
      "defaultValue": "RegenerationRun",
      "usedBy": ["A", "B", "C", "D"],
      "description": "Name of the run header list. Parameterised so a test deployment can point at its own copy without touching production data."
    },
    {
      "schemaName": "fi_WorkItemListName",
      "displayName": "Work Item List Name",
      "type": "String",
      "required": true,
      "defaultValue": "RegenerationWorkItem",
      "usedBy": ["A", "B", "C", "D"],
      "description": "Name of the work queue list. Parameterised so a test deployment can point at its own copy without touching production data."
    },
    {
      "schemaName": "fi_WalkFrontierListName",
      "displayName": "Walk Frontier List Name",
      "type": "String",
      "required": true,
      "defaultValue": "WalkFrontier",
      "usedBy": ["E"],
      "description": "Holds the folder walk frontier. Persisted rather than held in an array variable so the walk is resumable, observable, and immune to the 16 MB action output cap."
    },
    {
      "schemaName": "fi_IndexWalkRunListName",
      "displayName": "Index Walk Run List Name",
      "type": "String",
      "required": true,
      "defaultValue": "IndexWalkRun",
      "usedBy": ["A", "E"],
      "description": "Tracks whether a full backfill and stale-row reconciliation completed successfully."
    }
  ],
  "connectionReferences": [
    {
      "schemaName": "fi_sharedsharepointonline",
      "displayName": "SharePoint (regeneration solution)",
      "connectorId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
      "description": "Standard SharePoint connector. Used by every flow for reads, writes and the folder walk. No premium connector is used anywhere in this solution."
    },
    {
      "schemaName": "fi_sharedagentnode",
      "displayName": "Agent (regeneration solution)",
      "connectorId": "/providers/Microsoft.PowerApps/apis/shared_agentnode",
      "description": "Used only by Flow B to invoke the existing PD Conversion Assistant agent by schema name."
    }
  ]
}
